---
"looped-whisper": patch
---

Hide the Model and API key fields in Settings → Rewrite when the on-device provider is selected. They don't apply to it, and two empty boxes read as "something is missing" when nothing is. The availability status is shown there now too, matching Settings → Style.
