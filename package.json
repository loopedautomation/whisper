{
  "name": "looped-whisper",
  "version": "0.0.0",
  "private": true,
  "description": "Free, open-source, local voice transcription for macOS.",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "changelog": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
